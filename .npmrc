# Production deploy: skip devDependencies (puppeteer, xlsx) — not needed at
# runtime, and puppeteer's Chromium download fails under cPanel's installer.
# For local dev tooling (screenshots, data scripts) run: npm install --include=dev
omit=dev
